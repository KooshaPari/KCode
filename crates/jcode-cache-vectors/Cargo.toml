[package]
name = "jcode-cache-vectors"
version = "0.1.0"
edition = "2024"
publish = false
description = "Prompt-cache vector hashing and cache-break detection for Jcode"

[lib]
name = "jcode_cache_vectors"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
